# revng dependency

`root` contains the local revng build from orchestra.

The lifter uses:

- `root/bin/python3.14`
- `root/bin/revng2`
- revng libraries and Python packages under `root`

Set `REVNG_ROOT` to point at another orchestra root if needed.
